# time-to-move
Pomodoro like app


To run in localhost, you need Node.js installed.
Then install the dependencies in the project folder.

```sh
npm install
```

To run the app:

```sh
npm run dev
```
